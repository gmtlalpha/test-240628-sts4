<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.rho.wish.model.WishMapper">
    
    <insert id="createWish" parameterType="com.rho.wish.model.Wish">
        INSERT INTO wish (
	        id,
	        itemName,
	        itemCategory,
	        itemCode
        ) VALUES (
	        id_seq.nextval,
	        #{itemName},
	        #{itemCategory},
	        #{itemCode}
        )
    </insert>
    
    <select id="readWishList" resultType="com.rho.wish.model.Wish">
        SELECT * FROM wish ORDER BY id
    </select>
    
    <select id="readWishById" parameterType="long" resultType="com.rho.wish.model.Wish">
        SELECT * FROM wish WHERE id = #{id}
    </select>

    <update id="updateWishById" parameterType="long">
        UPDATE wish SET 
	        itemName = #{itemName}, 
	        itemCategory = #{itemCategory}, 
	        itemCode = #{itemCode}
        WHERE id = #{pId}
    </update>

    <delete id="deleteWishById" parameterType="long">
        DELETE FROM wish WHERE id = #{id}
    </delete>
    
</mapper>